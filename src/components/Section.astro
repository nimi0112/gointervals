---
interface Props {
  title: string;
  id?: string;
  flush?: boolean;
}
const { title, id, flush = false } = Astro.props;
---

<section class:list={['section', { 'section--flush': flush }]} id={id}>
  <h2>{title}</h2>
  <div class="prose">
    <slot />
  </div>
</section>
