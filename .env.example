# Public site URL, no trailing slash. Used for canonicals, sitemap, OG image URLs.
PUBLIC_SITE_URL=https://gointervals.com

# Google Analytics 4 measurement ID. GA4 Admin -> Data streams -> your web stream -> Measurement ID.
# Leave empty to disable analytics entirely (no script is loaded).
PUBLIC_GA_MEASUREMENT_ID=
